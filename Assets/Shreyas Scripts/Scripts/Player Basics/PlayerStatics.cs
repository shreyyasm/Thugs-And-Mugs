using Dhiraj;
using UnityEngine;

public class PlayerStatics : Singleton<PlayerStatics>
{
    
    public int Woods;
    public int Money;
    public int BarReputation;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
   
}
